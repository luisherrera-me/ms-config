report:
  template: "{DataSensors} was founded in {creation_date} by {creation} the websites from this company are {sensor}"

fallback:
  uri: "http://localhost:8787/api-sersor-fallback/sensor_history/{id}"